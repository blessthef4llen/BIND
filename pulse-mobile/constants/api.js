// ─────────────────────────────────────────────────────────────────
//  BEFORE THE DEMO: set this to your machine's LAN IP
//  Mac/Linux:  run `ifconfig | grep "inet "` → look for 192.168.x.x
//  Windows:    run `ipconfig`               → look for IPv4 Address
//
//  localhost and 127.0.0.1 do NOT work on a physical device.
// ─────────────────────────────────────────────────────────────────

export const API_BASE = 'http://192.168.0.247:8000/api';

// Example: export const API_BASE = 'http://192.168.1.42:8000/api';
